<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_foster"
    app:startDestination="@id/loginActivity">

    <activity
        android:id="@+id/loginActivity"
        android:name="com.example.foster.LoginActivity"
        android:label="activity_login"
        tools:layout="@layout/activity_login" />
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.foster.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/musicActivity"
        android:name="com.example.foster.MusicActivity"
        android:label="activity_music"
        tools:layout="@layout/activity_music" />
    <activity
        android:id="@+id/workoutActivity"
        android:name="com.example.foster.WorkoutActivity"
        android:label="content_scrolling"
        tools:layout="@layout/content_scrolling" />
    <activity
        android:id="@+id/startActivity"
        android:name="com.example.foster.StartActivity"
        android:label="activity_start"
        tools:layout="@layout/activity_start" />
</navigation>